# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/gateway.networking.k8s.io/listenerset_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
metadata:
  name: webhook
  annotations:
    cert-manager.io/cluster-issuer: cert-manager-public-prod
spec:
  parentRef:
    namespace: istio-system
    name: public
    kind: Gateway
    group: gateway.networking.k8s.io
  listeners:
    - name: https
      hostname: "flux-webhook.${PUBLIC_DOMAIN}"
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
