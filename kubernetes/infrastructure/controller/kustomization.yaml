---
# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: infrastructure-controller-
namespace: flux-system
resources:
  - ./cert-manager/ks.yaml
  - ./cnpg-system/ks.yaml
  - ./gateway/ks.yaml
  - ./istio-system/ks.yaml
  - ./keycloak/ks.yaml
  - ./monitoring/ks.yaml
  - ./reloader/ks.yaml

  # External usage: my projects,...
  # Maybe disabled when I don't need them
  # - ./atlas-operator/ks.yaml
  - ./elastic-system/ks.yaml
  - ./minio-operator/ks.yaml
configurations:
  - kustomizeconfig.yaml
