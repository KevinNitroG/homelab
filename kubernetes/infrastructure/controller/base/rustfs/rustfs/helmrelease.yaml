---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name rustfs
spec:
  interval: 12h
  timeout: 1m
  chart:
    spec:
      chart: *name
      sourceRef:
        kind: HelmRepository
        name: *name
      version: 0.0.98
  install:
    crds: CreateReplace
  valuesFrom:
    - kind: ConfigMap
      name: rustfs-helm-values
