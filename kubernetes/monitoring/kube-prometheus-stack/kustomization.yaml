---
# yaml-language-server: $schema=https://www.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helm-release.yaml
configMapGenerator:
  - name: kube-prometheus-stack-helm-value
    files:
      - ./values.yaml
configurations:
  - ./kustomizeconfig.yaml
