---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/postgresql.cnpg.io/cluster_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: &name cluster
spec:
  instances: 1
  storage:
    size: 200Mi
  monitoring:
    enablePodMonitor: false
  bootstrap:
    initdb:
      database: &cloudbeaver cloudbeaver
      secret:
        name: *cloudbeaver
  managed:
    services:
      disabledDefaultServices:
        - ro
        - r
      additional:
        - selectorType: rw
          serviceTemplate:
            metadata:
              name: *name
