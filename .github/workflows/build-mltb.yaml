name: Build MLTB

on:
  workflow_dispatch:
  push:
    paths:
      - submodules/mltb/anasty17-mltb

jobs:
  build:
    permissions:
      packages: write
    uses: ./.github/workflows/build-image.yaml
    with:
      app_dir: submodules/mltb/anasty17-mltb
      image_name: mltb
