name: Build MLTB

on:
  workflow_dispatch:

jobs:
  build-mltb:
    permissions:
      packages: write
    uses: ./.github/workflows/build-image.yaml
    with:
      app_dir: kubernetes/apps/mltb/anasty17-mltb
      image_name: mltb
