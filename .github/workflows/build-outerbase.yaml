name: Build Outerbase

on:
  workflow_dispatch:

jobs:
  build:
    permissions:
      packages: write
    uses: ./.github/workflows/build-image.yaml
    with:
      app_dir: kubernetes/apps/outerbase/outerbase
      image_name: outerbase
