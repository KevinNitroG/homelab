name: Build Outerbase

on:
  workflow_dispatch:

jobs:
  build-mltb:
    uses: ./.github/workflows/build-image.yaml
    with:
      repo_dir: kubernetes/apps/outerbase/outerbase
      image_name: outerbase
