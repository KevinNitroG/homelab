[env]
_.file = ".env.yaml"
SOPS_AGE_KEY_FILE = "{{ config_root }}/secret/age.agekey"

[settings]
sops = { age_key_file = "{{ config_root }}/secret/age.agekey" }

# Renovate cannot track will mark as # !R

[tools]
"github:cloudnative-pg/cloudnative-pg" = "1.30.0" # !R
"age" = "1.3.2"
cmctl = "2.5.0"
flux2 = "2.9.4"
gh = "2.98.0"
kustomize = "5.8.1"
sops = "3.13.3"
terraform = "1.16.0"
uv = "0.12.7"
istioctl = "1.30.4"
