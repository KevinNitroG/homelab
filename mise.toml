[env]
_.file = ".env.yaml"
SOPS_AGE_KEY_FILE = "{{ config_root }}/secret/age.agekey"

[settings]
sops = { age_key_file = "{{ config_root }}/secret/age.agekey" }

# Renovate cannot track will mark as # !R

[tools]
"github:cloudnative-pg/cloudnative-pg" = "1.30.1" # !R
"age" = "1.3.2"
cmctl = "2.6.1"
flux2 = "2.9.5"
gh = "2.101.0"
kustomize = "5.8.1"
sops = "3.13.3"
terraform = "1.16.3"
uv = "0.12.17"
istioctl = "1.31.1"
